import { StyleSheet } from 'react-native';
import { theme } from '@src/config';


export default StyleSheet.create({
  safeAreaContainer: {
    backgroundColor: theme.OSStatusBarBackground,
  },
});

